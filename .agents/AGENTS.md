# Project Rules

- This project is built and run using Docker. Always build, run, or verify using Docker commands (e.g., `docker compose up --build` or `docker compose down`) instead of native host-level `npm` or `node` command runners.
