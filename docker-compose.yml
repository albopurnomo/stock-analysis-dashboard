version: '3.8'

services:
  web:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Useful for hot-reloading on Windows/Docker
